import FeaturePlaceholder from "@/components/FeaturePlaceholder";

export default function ReferencesPage() {
  return <FeaturePlaceholder badge="Administration" title="Références" description="Gérez les tables de référence utilisées dans l’application." />;
}
